{
  "name": "node-with-ts",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx watch server.ts",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t lars1595/pub-sub-kafka-server:latest .",
    "docker:push": "docker push lars1595/pub-sub-kafka-server:latest",
    "docker:buildpush": "npm run docker:build && npm run docker:push"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "*",
    "tslint": "*",
    "typescript": "*"
  },
  "dependencies": {
    "express": "*",
    "kafka-node": "^5.0.0",
    "websocket-express": "^1.3.13"
  }
}
