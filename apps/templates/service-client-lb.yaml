apiVersion: v1
kind: Service
metadata:
  name: service-client-lb
spec:
  selector:
    app: client
  type: LoadBalancer
  ports:
    - port: {{ .Values.client.port }}
      targetPort: {{ .Values.client.port }}
